#!/bin/sh
set -e
#Uncomment this if you have perl
#perl -V > /dev/null 2>&1
#if [ $? -eq 0 ]; then
# perl /usr/bin/mindelmod.pl /etc/min.conf ModuleName=/usr/lib/HardCodedTestModuleXXX.so
#else
# echo "Can not remove test module from min.conf (need perl)\n"
#fi
