The @code{resume} keyword is used to resume a paused test case. @code{resume} has one mandatory argument, described below.
@table @b
@item Argument
@item testid
The test ID from the run command.
@end table
The resume keyword can be used for, example, in the following way:
@example
resume test1
@end example
