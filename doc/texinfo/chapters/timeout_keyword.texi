The @code{timeout} keyword is used to give a timeout value for a test case. The timeout value is given as an argument for the @code{timeout} keyword, as described in Table 9-5 below.

@b{Argument}
@table @b
@item Timeout value
The timeout value in milliseconds.
@end table

The timeout keyword can be used, for example, in the following way (timeout 10 seconds):
@example
timeout 10000 
@end example
